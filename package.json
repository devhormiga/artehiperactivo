{
  "name": "artehiperactivo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy2": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup --dotfiles"

  },
  "dependencies": {
    "@fortawesome/vue-fontawesome": "^2.0.6",
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7",
    "nuxt-buefy": "^0.4.8",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "sass": "^1.50.1",
    "sass-loader": "^10.2.1"
  }
}
